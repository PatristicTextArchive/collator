[project]
name = "pta-collator"
version = "0.1.0"
description = "Collates transcriptions for PTA"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docopt==0.6.2",
    "ipykernel>=6.29.5",
    "pandas>=2.3.1",
]
